import React from "react";

// allows data to be passed down through the component tree without having to pass down props 
const DataAreaContext = React.createContext({

});

export default DataAreaContext;